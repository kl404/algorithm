/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var permute = function(nums) {
    


    const res=[];
    const selected=new Array(nums.length).fill(false);

    function dfs(state){
        if()
    }
    
    return res;
};